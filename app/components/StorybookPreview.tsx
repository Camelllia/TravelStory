import React from 'react';
import { View, Text } from 'react-native';

export default function StorybookPreview() {
  return (
    <View style={{ margin: 20 }}>
      <Text>PDF 기능은 현재 지원되지 않습니다. (추후 제공 예정)</Text>
    </View>
  );
}
